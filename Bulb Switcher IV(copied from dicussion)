class Solution:
    def minFlips(self, target: str) -> int:
        x=target[0]
        c=int(x)
        for i in target:
            if(i!=x):
                c=c+1
                x=i
        return c
        
