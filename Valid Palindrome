class Solution:
    def isPalindrome(self, s: str) -> bool:
        li=list(s)
        st=''
        for i in li:
            if i.isalpha() or i.isalnum():
                st+=i.lower()
        if st==st[::-1]:
            return True
        else:
            return False
                
