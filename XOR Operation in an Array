class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        li=[start]
        while len(li)!=n:
            start+=2
            li.append(start)
        # print(li)
        x=0
        for i in li:
            x=x^i
        return x
        
