class Solution:
    def getMinDistance(self, nums: List[int], target: int, start: int) -> int:
        li=[]
        if target in nums:
            for i in range(len(nums)):
                if nums[i]==target:
                    li.append(abs(i-start))
        return min(li)
            
        
