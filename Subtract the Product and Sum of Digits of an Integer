class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        prod=1
        sm=0
        for i in str(n):
            prod*=int(i)
            sm+=int(i)
        return prod-sm
        
        
