class Solution:
    def transpose(self, A: List[List[int]]) -> List[List[int]]:
        columns=[]
        for i in range(len(A[0])):
            c=[]
            for j in range(len(A)):
                c.append(A[j][i])
            columns.append(c)
        return columns
                
        
        
